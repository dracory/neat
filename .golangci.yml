version: "2"

run:
  timeout: 5m

linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - unused

linters-settings:
  govet:
    settings:
      inline:
        enabled: false

issues:
  exclude-rules:
    - linters:
        - govet
      text: "inline:"
